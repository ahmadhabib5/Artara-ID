<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ArtaraID" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/brown_primary</item>
        <item name="colorPrimaryVariant">@color/brown_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.ArtaraID.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.ArtaraID.FullScreen">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="MyBtn" parent="Widget.AppCompat.Button">
        <item name="android:textSize">18sp</item>
        <item name="android:background">@drawable/bg_btn</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="MyBtn.continue" parent="MyBtn">
        <item name="android:text">@string/continue_str</item>
    </style>

    <style name="MyBtn.login" parent="MyBtn">
        <item name="android:text">@string/login</item>
    </style>
    <style name="MyBtn.open" parent="MyBtn">
        <item name="android:background">@drawable/bg_btn_with_border</item>
    </style>
    <style name="MyBtn2" parent="MyBtn">
        <item name="android:background">@drawable/bg_btn_with_border</item>
    </style>
    <style name="MyBtn.predict" parent="MyBtn">
        <item name="android:background">@drawable/bg_btn_with_border</item>
    </style>
    <style name="MyBtn.save" parent="MyBtn">
        <item name="android:background">@drawable/bg_btn_with_border</item>
    </style>

    <style name="menu">
        <item name="android:layout_width">120dp</item>
        <item name="android:layout_height">200dp</item>
        <item name="android:orientation">vertical</item>
        <item name="android:paddingTop">20dp</item>
        <item name="android:paddingStart">10dp</item>
        <item name="android:paddingEnd">10dp</item>
        <item name="android:paddingBottom">20dp</item>
        <item name="android:background">@drawable/bg_menu</item>
        <item name="android:clickable">true</item>
    </style>

    <style name="menu.predict">
        <item name="android:layout_width">200dp</item>
        <item name="android:layout_height">200dp</item>
    </style>

    <style name="text" parent="Widget.AppCompat.TextView">
        <item name="fontFamily">@font/araboto_light_400</item>
    </style>
    <style name="text.bold">
        <item name="fontFamily">@font/araboto_bold_400</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="TransparentText" parent="@android:style/TextAppearance">
        <item name="android:textColor">#00000000</item>
    </style>

    <style name="myEditText">
        <item name="android:background">@drawable/bg_input_combine</item>
        <item name="android:padding">15dp</item>
        <item name="android:drawablePadding">10dp</item>
        <item name="android:textColorHighlight">@color/brown_primary</item>
        <item name="android:textColorHint">@color/white_primary</item>
        <item name="android:textColorLink">@color/brown_primary</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="myEditText.email">
        <item name="android:hint">@string/email</item>
        <item name="android:drawableStart">@drawable/ic_email</item>
        <item name="android:inputType">textEmailAddress</item>
    </style>
    <style name="myEditText.username">
        <item name="android:hint">@string/username</item>
        <item name="android:drawableStart">@drawable/ic_person</item>
        <item name="android:inputType">text</item>
    </style>
    <style name="myEditText.password">
        <item name="android:hint">@string/password</item>
        <item name="android:drawableStart">@drawable/ic_lock</item>
        <item name="android:inputType">textPassword</item>
    </style>
    <style name="myEditText.rePassword">
        <item name="android:hint">@string/rePassword</item>
        <item name="android:drawableStart">@drawable/ic_lock</item>
        <item name="android:inputType">textPassword</item>
    </style>


</resources>